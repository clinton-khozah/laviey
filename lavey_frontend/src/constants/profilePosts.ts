/** Max photos on profile timeline (matches backend) */
export const MAX_PROFILE_POSTS = 5;

export const POST_LIMIT_MESSAGE = `You can have at most ${MAX_PROFILE_POSTS} posts on your profile. Delete one to add another.`;
