{
  "name": "lavey-frontend",
  "private": true,
  "description": "Lavey frontend — deployed on Netlify. API lives in a separate backend repo.",
  "scripts": {
    "dev": "npm run dev --prefix lavey_frontend",
    "build": "npm run build --prefix lavey_frontend",
    "lint": "npm run lint --prefix lavey_frontend",
    "preview": "npm run preview --prefix lavey_frontend"
  }
}
