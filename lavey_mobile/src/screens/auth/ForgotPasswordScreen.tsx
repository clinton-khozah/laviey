import { useState } from 'react'; import { Alert } from 'react-native'; import { AuthLayout } from './AuthLayout'; import { InputField } from '../../components/common/InputField'; import { PrimaryButton } from '../../components/common/PrimaryButton'; import { isEmail } from '../../utils/validators';
export function ForgotPasswordScreen() { const [email, setEmail] = useState(''); const submit = () => { if (!isEmail(email)) return Alert.alert('Email required', 'Enter a valid email address.'); Alert.alert('Recovery not enabled yet', 'The shared Lavey backend does not currently expose a password-recovery endpoint. Please contact support or add the endpoint before release.'); }; return <AuthLayout title="Reset password" subtitle="Enter the email connected to your Lavey account."><InputField label="Email address" value={email} onChangeText={setEmail} autoCapitalize="none" keyboardType="email-address" /><PrimaryButton label="Send reset link" onPress={submit} /></AuthLayout>; }
