import { memo } from 'react'; import { Pressable, StyleSheet, Text, View } from 'react-native'; import { Image } from 'expo-image'; import { LinearGradient } from 'expo-linear-gradient'; import { Ionicons } from '@expo/vector-icons'; import { theme } from '../../../constants/theme'; import type { Profile } from '../../../types';
function Card({ profile, onPress }: { profile: Profile; onPress(): void }) { return <Pressable style={styles.card} onPress={onPress}><Image source={{ uri: profile.avatar }} contentFit="cover" cachePolicy="memory-disk" style={StyleSheet.absoluteFill} /><LinearGradient colors={['transparent', 'rgba(0,0,0,.72)']} style={StyleSheet.absoluteFill} /><View style={styles.info}><View style={styles.row}><Text numberOfLines={1} style={styles.name}>{profile.name}, {profile.age}</Text>{profile.verified ? <Ionicons name="checkmark-circle" size={15} color={theme.colors.teal} /> : null}</View><Text style={styles.distance}>{profile.distance}</Text></View></Pressable>; }
export const UserGridCard = memo(Card);
const styles = StyleSheet.create({ card: { width: '48.5%', aspectRatio: 0.75, borderRadius: 18, overflow: 'hidden', backgroundColor: theme.colors.surfaceMuted, ...theme.shadow }, info: { position: 'absolute', left: 12, right: 12, bottom: 12 }, row: { flexDirection: 'row', alignItems: 'center', gap: 4 }, name: { color: theme.colors.white, fontFamily: theme.typography.semibold, fontSize: 15, flexShrink: 1 }, distance: { color: 'rgba(255,255,255,.76)', fontFamily: theme.typography.regular, fontSize: 11, marginTop: 1 } });
