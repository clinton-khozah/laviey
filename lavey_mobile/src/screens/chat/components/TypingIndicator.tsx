import { useEffect } from 'react'; import { StyleSheet, View } from 'react-native'; import Animated, { useAnimatedStyle, useSharedValue, withRepeat, withSequence, withTiming } from 'react-native-reanimated'; import { theme } from '../../../constants/theme';
function Dot({ delay }: { delay: number }) { const opacity = useSharedValue(0.25); useEffect(() => { const timer = setTimeout(() => { opacity.value = withRepeat(withSequence(withTiming(1, { duration: 380 }), withTiming(.25, { duration: 380 })), -1); }, delay); return () => clearTimeout(timer); }, [delay, opacity]); return <Animated.View style={[styles.dot, useAnimatedStyle(() => ({ opacity: opacity.value }))]} />; }
export function TypingIndicator() { return <View style={styles.wrap}><Dot delay={0} /><Dot delay={140} /><Dot delay={280} /></View>; }
const styles = StyleSheet.create({ wrap: { flexDirection: 'row', gap: 4, alignSelf: 'flex-start', paddingHorizontal: 14, paddingVertical: 12, backgroundColor: theme.colors.surface, borderRadius: 18, borderBottomLeftRadius: 5 }, dot: { width: 6, height: 6, borderRadius: 3, backgroundColor: theme.colors.primary } });
