import { memo } from 'react'; import { StyleSheet, Text, View } from 'react-native'; import { Ionicons } from '@expo/vector-icons'; import { theme } from '../../../constants/theme'; import type { ChatMessage } from '../../../types';
function Bubble({ message }: { message: ChatMessage }) { const mine = message.senderId === 'me'; return <View style={[styles.row, mine && styles.myRow]}><View style={[styles.bubble, mine ? styles.mine : styles.theirs]}><Text style={[styles.text, mine && styles.myText]}>{message.text}</Text><View style={styles.meta}><Text style={[styles.time, mine && styles.myMeta]}>{message.sentAt}</Text>{mine ? message.read ? <Text style={{ color: theme.colors.teal, fontSize: 11 }}>✓✓✓</Text> : <Ionicons name="checkmark" size={14} color="rgba(255,255,255,.7)" /> : null}</View></View></View>; }
export const MessageBubble = memo(Bubble);
const styles = StyleSheet.create({ row: { alignItems: 'flex-start', marginVertical: 4 }, myRow: { alignItems: 'flex-end' }, bubble: { maxWidth: '82%', borderRadius: 18, paddingHorizontal: 14, paddingVertical: 10 }, mine: { backgroundColor: theme.colors.primary, borderBottomRightRadius: 5 }, theirs: { backgroundColor: theme.colors.surface, borderBottomLeftRadius: 5, borderWidth: 1, borderColor: theme.colors.border }, text: { fontFamily: theme.typography.regular, color: theme.colors.text, fontSize: 14, lineHeight: 20 }, myText: { color: theme.colors.white }, meta: { flexDirection: 'row', alignItems: 'center', justifyContent: 'flex-end', gap: 3, marginTop: 3 }, time: { fontFamily: theme.typography.regular, color: theme.colors.textMuted, fontSize: 9 }, myMeta: { color: 'rgba(255,255,255,.65)' } });
