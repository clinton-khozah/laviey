import { memo } from 'react'; import { Pressable, StyleSheet, Text, View } from 'react-native'; import { Image } from 'expo-image'; import { Ionicons } from '@expo/vector-icons'; import { theme } from '../../../constants/theme'; import type { Conversation } from '../../../types';
function Tile({ match, onPress }: { match: Conversation; onPress(): void }) { return <Pressable style={styles.tile} onPress={onPress}><View><Image source={{ uri: match.participantAvatar }} style={styles.avatar} contentFit="cover" cachePolicy="memory-disk" />{match.isOnline ? <View style={styles.online} /> : null}</View><View style={styles.body}><View style={styles.row}><Text style={styles.name}>{match.participantName}</Text><Text style={styles.score}>{match.vibeScore}%</Text></View><Text numberOfLines={1} style={styles.preview}>{match.isTyping ? 'Typing…' : match.lastMessage || 'You matched — say hello!'}</Text></View><Ionicons name="chevron-forward" size={18} color={theme.colors.textMuted} /></Pressable>; }
export const MatchTile = memo(Tile);
const styles = StyleSheet.create({ tile: { flexDirection: 'row', alignItems: 'center', padding: 12, borderRadius: 18, backgroundColor: theme.colors.surface, gap: 12, ...theme.shadow }, avatar: { width: 62, height: 62, borderRadius: 31 }, online: { position: 'absolute', width: 14, height: 14, borderRadius: 7, backgroundColor: theme.colors.teal, borderWidth: 2, borderColor: theme.colors.surface, right: 1, bottom: 1 }, body: { flex: 1 }, row: { flexDirection: 'row', alignItems: 'center', gap: 8 }, name: { fontFamily: theme.typography.semibold, color: theme.colors.text, fontSize: 16, flex: 1 }, score: { fontFamily: theme.typography.semibold, color: theme.colors.primary, fontSize: 11, backgroundColor: theme.colors.primarySoft, borderRadius: 10, paddingHorizontal: 7, paddingVertical: 2 }, preview: { fontFamily: theme.typography.regular, color: theme.colors.textSecondary, fontSize: 12, marginTop: 4 } });
